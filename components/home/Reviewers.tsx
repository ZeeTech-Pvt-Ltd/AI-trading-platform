import Section from './Section';

/** Editorial team / reviewer bios — content added in a later prompt. */
export default function Reviewers() {
  return (
    <Section id="reviewers" className="home-section--reviewers">
      {/* Reviewer bios go here. */}
    </Section>
  );
}
