import Section from './Section';

/** Homepage footer slot — closing links added in a later prompt. */
export default function HomeFooter() {
  return (
    <Section id="footer" className="home-section--footer">
      {/* Footer links go here. */}
    </Section>
  );
}
