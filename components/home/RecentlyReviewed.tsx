import Section from './Section';

/** Latest review cards — content added in a later prompt. */
export default function RecentlyReviewed() {
  return (
    <Section id="recently-reviewed" className="home-section--recently-reviewed">
      {/* Recent review cards go here. */}
    </Section>
  );
}
