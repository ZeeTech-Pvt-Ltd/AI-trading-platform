import Section from './Section';

/** How we review — content added in a later prompt. */
export default function Methodology() {
  return (
    <Section id="methodology" className="home-section--methodology">
      {/* Methodology content goes here. */}
    </Section>
  );
}
