import Section from './Section';

/** Platforms flagged high-risk — content added in a later prompt. */
export default function HighRisk() {
  return (
    <Section id="high-risk" className="home-section--high-risk">
      {/* High-risk platform warnings go here. */}
    </Section>
  );
}
