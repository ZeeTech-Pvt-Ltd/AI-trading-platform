import Section from './Section';

/** Educational guides — content added in a later prompt. */
export default function Guides() {
  return (
    <Section id="guides" className="home-section--guides">
      {/* Guide links go here. */}
    </Section>
  );
}
