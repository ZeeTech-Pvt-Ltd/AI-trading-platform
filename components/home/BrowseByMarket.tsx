import Section from './Section';

/** Entry points by market (UK, AU, EU, CA, US, ZA, IN) — content added in a later prompt. */
export default function BrowseByMarket() {
  return (
    <Section id="browse-by-market" className="home-section--browse-by-market">
      {/* Market entry points go here. */}
    </Section>
  );
}
